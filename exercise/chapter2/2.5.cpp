#include <iostream>
#include <vector>
#include <string>
#include <stdlib.h>
using namespace std;


int max(int ix, int jx)
{
    return ix > jx ? ix : jx;
}

int max(float ix, float jx)
{
    return ix > jx ? ix : jx;
}

int max(string ix, string jx)
{
    //strcmp(ix.c_str(), jx.c_str());
}

int max(vector<int> ix)
{
    return 0;
}

int max(int num,int array[num])
{
    return 0;
}

{
    return ix > jx ? ix : jx;
}

int max(float ix, float jx)
{
    return ix > jx ? ix : jx;
}

int max(int ix, int jx)
{
    return ix > jx ? ix : jx;
}

int max(float ix, float jx)
{
    return ix > jx ? ix : jx;
}

